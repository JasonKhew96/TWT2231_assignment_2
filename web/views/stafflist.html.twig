{% extends 'base.html.twig' %}
{% block body %}
    <ul class="nav nav-tabs justify-content-center">
        <li class="nav-item">
            <a class="nav-link" href="{{ app.url_generator.generate('index') }}">Home</a>
        </li>
        <li class="nav-item">
            <a class="nav-link active" href="stafflist.php">Staff List</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="profile.php">Profile</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="payroll.php">Payroll</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ app.url_generator.generate('login') }}">Login</a>
        </li>
    </ul>

    {% for staff in stafflists %}
        <staff>
            <p>{{ staff.staff_id }}</p>
            <p>{{ staff.first_name }}</p>
            <p>{{ staff.last_name }}</p>
        </staff>
    {% endfor %}
{% endblock %}
